
describe('Authentication flow', () => {
  test('It is should todo autentication. ', () => {
    expect(1).toEqual(1)
  })

  test('It is should return a token JWT. ', () => {
    expect(2).toEqual(2)
  })

  test('It is should a token JWT valid', () => {
    expect(3).toEqual(3)
  })
})
